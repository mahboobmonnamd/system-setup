# Freelance identity — machine-local. scripts/git_setup.sh materializes this
# (drops .example, fills in real values). The real file is gitignored.
[user]
	name = Your Name
	email = you@freelance.example
[core]
	sshCommand = ssh -i ~/.ssh/id_ed25519_freelance -o IdentitiesOnly=yes
